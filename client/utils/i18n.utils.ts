import { DynamicObject, Dynamic } from '../interfaces/common.interfaces';

export const getMessage = (messages: DynamicObject<string>, key: string, params?: Dynamic<string>): string => {
	return '';
};
